# Team Water — environment configuration
# Copy to .env (or set in your host's environment). Values shown are defaults unless marked REQUIRED.

# ── Server ──────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
# Set to 1 when running behind a reverse proxy / load balancer (needed for correct client IPs in rate limits).
TRUST_PROXY=0
# DATA_DIR holds the SQLite database and uploads. Back it up. Must be on persistent storage.
DATA_DIR=data/runtime

# ── Secrets (REQUIRED in production) ─────────────────────────────────────
# 64 hex chars each. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# PHONE_ENC_KEY encrypts reporter phone numbers (AES-256-GCM). Losing it makes stored numbers unreadable.
PHONE_ENC_KEY=
# HMAC_KEY keys phone lookups and rate-limit hashes.
HMAC_KEY=
SESSION_HOURS=12

# ── First administrator (used by `npm run setup` when no admin exists) ──
ADMIN_USERNAME=admin
# Leave empty to have a strong password generated and printed once.
ADMIN_PASSWORD=

# ── Base map ─────────────────────────────────────────────────────────────
# osm (default, no key) or google (Google Maps JavaScript API — key + billing).
MAP_PROVIDER=osm
# OSM's public tiles are for light use only. For production traffic use a commercial or self-hosted
# tile service (e.g. MapTiler, Stadia, Thunderforest) and put its URL + attribution here.
MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
MAP_ATTRIBUTION=
MAP_PROVIDER_NAME=
# Google (only when MAP_PROVIDER=google). Browser key MUST be HTTP-referrer restricted to your domain.
GOOGLE_MAPS_BROWSER_KEY=
GOOGLE_MAPS_MAP_ID=
# Server-side key for Geocoding / Routes (IP-restricted; never sent to browsers).
GOOGLE_MAPS_SERVER_KEY=

# ── Geocoding (location search) ──────────────────────────────────────────
# nominatim | google | none. Local area aliases (English/Urdu/Roman Urdu) always work without a provider.
GEOCODER_PROVIDER=nominatim
NOMINATIM_URL=https://nominatim.openstreetmap.org
# Nominatim policy requires an identifying User-Agent with contact details.
GEOCODER_USER_AGENT=TeamWater-FaisalabadWaterFinder/1.0 (contact: you@example.org)
GEOCODER_EMAIL=

# ── Routing (travel distance/time) ───────────────────────────────────────
# osrm | google | none. Without routing, straight-line distance is used and labelled as such.
ROUTING_PROVIDER=osrm
# The public OSRM demo server is car-only and not for production. Run your own OSRM for real traffic.
OSRM_URL=https://router.project-osrm.org
OSRM_FOOT_URL=
OSRM_BIKE_URL=

# ── SMS phone verification ───────────────────────────────────────────────
# none (reports accepted as unverified) | twilio | console (DEVELOPMENT ONLY — prints codes to the log).
SMS_PROVIDER=none
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=

# ── Moderation (keep private) ────────────────────────────────────────────
RISK_REVIEW_THRESHOLD=40

# ── Uploads & retention ─────────────────────────────────────────────────
UPLOAD_MAX_PHOTO_MB=5
UPLOAD_MAX_PHOTOS=3
UPLOAD_MAX_DOC_MB=15
UPLOAD_MAX_IMPORT_MB=20
RETENTION_REPORT_DAYS=730
RETENTION_CONTACT_DAYS=180

# ── Demonstration data ───────────────────────────────────────────────────
# 1 = load clearly labelled DEMO plants (never real). Keep 0 in production.
DEMO_DATA=0
